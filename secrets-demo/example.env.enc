# Example SOPS-encrypted environment file
# This demonstrates the pattern - real secrets would be encrypted
#
# To create a real encrypted file with SOPS:
#   sops --encrypt --age <your-age-public-key> example.env > example.env.enc
#
# To edit:
#   sops example.env.enc
#
# To decrypt:
#   sops --decrypt example.env.enc
#
# Example unencrypted content (DO NOT store real secrets like this):
# DATABASE_PASSWORD=changeme
# API_KEY=your-api-key-here
# MQTT_USER=admin
# MQTT_PASSWORD=changeme
#
# This file would normally contain encrypted SOPS data like:
# {
#   "data": "ENC[AES256_GCM,data:...,iv:...,tag:...,type:str]",
#   "sops": {
#     "age": [...],
#     "lastmodified": "...",
#     "version": "3.8.0"
#   }
# }
